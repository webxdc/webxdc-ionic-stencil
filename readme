

limitations:

- use src attribute instead of name for icons, because using the name would include all icons in the build
   - (and we manually exlude them from the xdc because of that right now in `./create-webxdc.sh`)



# todo
- [ ] display "chats"
- [ ] allow setting topic name
- [ ] add option to use (if it works) with https://github.com/webxdc/webxdc-dev#during-development